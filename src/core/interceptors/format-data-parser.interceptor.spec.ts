import { FormatDataParserInterceptor } from './format-data-parser.interceptor';

describe('FormatDataParserInterceptor', () => {
  it('should be defined', () => {
    expect(new FormatDataParserInterceptor()).toBeDefined();
  });
});
